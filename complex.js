class SimplicialComplex {
    constructor(vertices=[]) {
  	this.vertices = vertices;
	this.simplices =[];
    }
    
    // Compute beta_1
    beta_1( ){ 
  	return 0;
    }
    
    // Compute beta_2
    beta_2( ) {
	
  	return 0;
    }
    
    // Draw Shadow
    shadow( ) {
	
	
    }
    // Compute Cech complex
    static cech(points,scale) {
	
	
    }
    
    // Compute Rips Complex
    static rips(points,scale) {
	
	
    }
}
